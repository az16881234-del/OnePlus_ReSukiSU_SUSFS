name: 构建一加内核（带 KPM）

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      device_config:
        description: '配置文件(设备) — 选择要构建的设备/系统'
        required: true
        type: choice
        default: OP-ACE-3V_OOS15
        options:
          - OP-ACE-3-PRO_OOS14
          - OP-ACE-RACE_OOS14
          - OP-ACE_OOS14
          - OP-PAD-PRO_OOS14
          - OP10pro_OOS14
          - OP10r_OOS14
          - OP11_OOS14
          - OP11r_OOS14
          - OP12_OOS14
          - OP-ACE-2-PRO_OOS15
          - OP-ACE-2-PRO_OOS16
          - OP-ACE-2V_OOS15
          - OP-ACE-2_OOS15
          - OP-ACE-2_OOS16
          - OP-ACE-3-PRO_OOS15
          - OP-ACE-3-PRO_OOS16
          - OP-ACE-3V_OOS15
          - OP-ACE-3V_OOS16
          - OP-ACE-3_OOS15
          - OP-ACE-3_OOS16
          - OP-ACE-5-PRO_OOS15
          - OP-ACE-5-PRO_OOS16
          - OP-ACE-5-RACE_OOS15
          - OP-ACE-5-RACE_OOS16
          - OP-ACE-5-ULTRA_OOS15
          - OP-ACE-5-ULTRA_OOS16
          - OP-ACE-5_OOS15
          - OP-ACE-5_OOS16
          - OP-ACE-6T_OOS16
          - OP-ACE-6_OOS16
          - OP-ACE-RACE_OOS15
          - OP-ACE_OOS15
          - OP-NORD-3_OOS15
          - OP-NORD-4-CE_OOS15
          - OP-NORD-4-CE_OOS16
          - OP-NORD-4_OOS15
          - OP-NORD-4_OOS16
          - OP-NORD-5_OOS15
          - OP-NORD-5_OOS16
          - OP-NORD-CE-5_OOS15
          - OP-NORD-CE-5_OOS16
          - OP-NORD-CE4-LITE_OOS15
          - OP-NORD-CE4-LITE_OOS16
          - OP-OPEN_OOS15
          - OP-OPEN_OOS16
          - OP-PAD-2-MT6991_OOS16
          - OP-PAD-2-PRO_OOS15
          - OP-PAD-2-PRO_OOS16
          - OP-PAD-2-SM8650_OOS15
          - OP-PAD-2-SM8650_OOS16
          - OP-PAD-3-MT6897_OOS15
          - OP-PAD-3-MT6897_OOS16
          - OP-PAD-3-SM8750_OOS15
          - OP-PAD-3-SM8750_OOS16
          - OP-PAD-GO-2_OOS16
          - OP-PAD-LITE_OOS15
          - OP-PAD-MT6983_OOS15
          - OP-PAD-MT6983_OOS16
          - OP-PAD-PRO_OOS15
          - OP-PAD-PRO_OOS16
          - OP-TURBO-6V_OOS16
          - OP-TURBO-6_OOS16
          - OP10pro_OOS15
          - OP10pro_OOS16
          - OP10r_OOS15
          - OP10t_OOS15
          - OP11_OOS15
          - OP11_OOS16
          - OP11r_OOS15
          - OP11r_OOS16
          - OP12_OOS15
          - OP12_OOS16
          - OP12r_OOS15
          - OP12r_OOS16
          - OP13-CPH_OOS15
          - OP13-PJZ_OOS15
          - OP13S_OOS15
          - OP13S_OOS16
          - OP13T_OOS15
          - OP13T_OOS16
          - OP13_OOS16
          - OP13r_OOS15
          - OP13r_OOS16
          - OP15T_OOS16
          - OP15_OOS16
          - OP15r_OOS16

      enable_kpm:
        description: '启用 KPM（内核补丁模块）'
        required: true
        type: boolean
        default: true

      ksu_type:
        description: 'KSU 类型（KPM 支持：ReSukiSU / SukiSU-Ultra）'
        required: true
        type: choice
        options:
          - ReSukiSU
          - SukiSU-Ultra
          - KSU
        default: ReSukiSU

      ksu_branch_or_hash:
        description: 'KSU 分支或提交哈希（默认 main）'
        required: false
        type: string
        default: main

      ksu_version_override:
        description: 'KSU 固定版本号（留空 = 按 git rev-list 自动算；示例：40545）'
        required: false
        type: string
        default: ''

      susfs_branch_or_hash:
        description: 'SusFS 分支或提交哈希（留空 = 按内核版本自动选）'
        required: false
        type: string
        default: ''

      optimize_level:
        description: '编译优化等级'
        required: true
        type: choice
        options: [O2, O3]
        default: O2

      clean_build:
        description: '清理构建（禁用 ccache，速度慢但保证干净）'
        required: false
        type: boolean
        default: false

      make_release:
        description: '是否创建 GitHub Release（含 Telegram 通知）'
        required: false
        type: boolean
        default: false

      debug:
        description: '调试模式（保留 vmlinux / build log 等中间产物）'
        required: false
        type: boolean
        default: false

      build_timestamp:
        description: '自定义 uname 时间戳（留空 = 当前时间），例如：Tue Feb 10 10:01:02 UTC 2026'
        required: false
        type: string
        default: ''

jobs:
  # ============================================================
  # 第 1 阶段：解析 device_config，加载 JSON，并校验 KPM 兼容性
  # ============================================================
  resolve-config:
    name: 📋 解析设备配置
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.gen.outputs.matrix }}
      ksu_resolved_hash: ${{ steps.gen.outputs.ksu_resolved_hash }}
      susfs_resolved_hash: ${{ steps.gen.outputs.susfs_resolved_hash }}
      susfs_version: ${{ steps.gen.outputs.susfs_version }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v6
        with:
          sparse-checkout: configs/
          sparse-checkout-cone-mode: false

      - name: 🛡️ KPM 与 KSU 类型校验
        run: |
          if [ "${{ inputs.enable_kpm }}" = "true" ] && [ "${{ inputs.ksu_type }}" = "KSU" ]; then
            echo "::error::KPM 不支持 tiann/KernelSU 上游。请改用 ReSukiSU 或 SukiSU-Ultra，或关闭 enable_kpm"
            exit 1
          fi
          echo "✅ KPM/KSU 组合校验通过（ksu_type=${{ inputs.ksu_type }}, enable_kpm=${{ inputs.enable_kpm }}）"

      - name: 🔍 加载设备 JSON 配置
        id: gen
        shell: bash
        run: |
          set -euo pipefail
          DEVICE="${{ inputs.device_config }}"
          # 解析格式：MODEL_OOSxx → model=MODEL, oos=oosxx (小写)
          MODEL="${DEVICE%_OOS*}"
          OOS_RAW="OOS${DEVICE##*_OOS}"
          OOS_LOWER="$(echo "$OOS_RAW" | tr '[:upper:]' '[:lower:]')"

          CONFIG_FILE="configs/${OOS_LOWER}/${MODEL}.json"
          if [ ! -f "$CONFIG_FILE" ]; then
            echo "::error::找不到配置文件 $CONFIG_FILE"
            echo "解析结果：model=$MODEL oos=$OOS_LOWER"
            exit 1
          fi
          echo "✅ 加载配置：$CONFIG_FILE"
          cat "$CONFIG_FILE"

          # 注入 ksu 字段，方便 build job 用
          DEVICE_JSON=$(jq -c \
            --arg t "${{ inputs.ksu_type }}" \
            --arg h "${{ inputs.ksu_branch_or_hash }}" \
            '. + {ksu_type: $t, ksu_hash: $h}' "$CONFIG_FILE")

          # 解析 KSU 哈希（main / 分支名 / 40-char hash 都接受）
          KSU_REF="${{ inputs.ksu_branch_or_hash }}"
          KSU_REF="${KSU_REF:-main}"
          case "${{ inputs.ksu_type }}" in
            ReSukiSU)      KSU_REPO="ReSukiSU/ReSukiSU" ;;
            SukiSU-Ultra)  KSU_REPO="SukiSU-Ultra/SukiSU-Ultra" ;;
            *)             KSU_REPO="tiann/KernelSU" ;;
          esac
          KSU_SHA=$(gh api "repos/$KSU_REPO/commits/$KSU_REF" --jq '.sha' 2>/dev/null || echo "$KSU_REF")
          echo "ksu_resolved_hash=$KSU_SHA" >> "$GITHUB_OUTPUT"
          echo "ℹ️ KSU 解析：$KSU_REPO @ $KSU_REF → $KSU_SHA"

          # 解析 SUSFS（如果设备 JSON susfs=true）
          SUSFS_ENABLED=$(jq -r '.susfs // false' "$CONFIG_FILE")
          if [ "$SUSFS_ENABLED" = "true" ]; then
            ANDROID_VER=$(jq -r '.android_version' "$CONFIG_FILE")
            KERNEL_VER=$(jq -r '.kernel_version' "$CONFIG_FILE")
            SUSFS_INPUT="${{ inputs.susfs_branch_or_hash }}"
            if [ -z "$SUSFS_INPUT" ]; then
              SUSFS_REF="gki-${ANDROID_VER}-${KERNEL_VER}"
            else
              SUSFS_REF="$SUSFS_INPUT"
            fi
            SUSFS_SHA=$(curl -sf "https://gitlab.com/api/v4/projects/simonpunk%2Fsusfs4ksu/repository/commits/${SUSFS_REF}" | jq -r '.id // empty')
            [ -z "$SUSFS_SHA" ] && SUSFS_SHA="$SUSFS_REF"
            echo "susfs_resolved_hash=$SUSFS_SHA" >> "$GITHUB_OUTPUT"
            echo "ℹ️ SUSFS 解析：$SUSFS_REF → $SUSFS_SHA"

            # 顺便从 susfs.h 抓 SUSFS_VERSION 用于 release tag
            SUSFS_VER=$(curl -sf "https://gitlab.com/simonpunk/susfs4ksu/-/raw/${SUSFS_REF}/kernel_patches/include/linux/susfs.h" \
              | grep '#define SUSFS_VERSION' | awk -F'"' '{print $2}' || echo "unknown")
            echo "susfs_version=$SUSFS_VER" >> "$GITHUB_OUTPUT"
            echo "ℹ️ SUSFS 版本：$SUSFS_VER"
          else
            echo "susfs_resolved_hash=" >> "$GITHUB_OUTPUT"
            echo "susfs_version=none" >> "$GITHUB_OUTPUT"
          fi

          # 把 device JSON + KSU/SUSFS 哈希塞进 matrix
          MATRIX=$(jq -nc --argjson dev "$DEVICE_JSON" \
            --arg ksu_sha "$KSU_SHA" \
            --arg susfs_sha "$SUSFS_SHA" \
            '{include: [$dev + {ksu_resolved_hash: $ksu_sha, susfs_resolved_hash: $susfs_sha}]}')
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"

      - name: 📊 构建计划摘要
        run: |
          {
            echo "## 🎯 构建计划"
            echo ""
            echo "| 项目 | 值 |"
            echo "|------|------|"
            echo "| **设备配置** | \`${{ inputs.device_config }}\` |"
            echo "| **KPM 启用** | ${{ inputs.enable_kpm && '✅ 是' || '❌ 否' }} |"
            echo "| **KSU 类型** | ${{ inputs.ksu_type }} |"
            echo "| **KSU 引用** | \`${{ inputs.ksu_branch_or_hash }}\` (\`${{ steps.gen.outputs.ksu_resolved_hash }}\`) |"
            if [ -n "${{ inputs.ksu_version_override }}" ]; then
              echo "| **KSU 固定版本号** | 🔒 \`${{ inputs.ksu_version_override }}\` |"
            else
              echo "| **KSU 固定版本号** | 🔢 自动（git rev-list 计算） |"
            fi
            echo "| **SUSFS 版本** | ${{ steps.gen.outputs.susfs_version }} |"
            echo "| **优化等级** | ${{ inputs.optimize_level }} |"
            echo "| **清理构建** | ${{ inputs.clean_build && '✅ 是' || '❌ 否' }} |"
            echo "| **创建 Release** | ${{ inputs.make_release && '✅ 是' || '❌ 否' }} |"
            echo "| **调试模式** | ${{ inputs.debug && '✅ 是' || '❌ 否' }} |"
          } >> "$GITHUB_STEP_SUMMARY"

  # ============================================================
  # 第 2 阶段：编译内核（调用现有 build-kernel composite action）
  # ============================================================
  build:
    name: 🔨 构建 (${{ matrix.model }}, ${{ matrix.os_version }})
    needs: resolve-config
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.resolve-config.outputs.matrix) }}
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: 🧹 磁盘清理（按需）
        if: ${{ matrix.disk_cleanup }}
        uses: ./.github/actions/disk-cleanup

      - name: 📦 安装最少依赖
        run: |
          set -euo pipefail
          echo "::group::Install dependencies"
          sudo apt-get -o Acquire::Retries=3 update -qq
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            git curl ca-certificates build-essential clang lld flex bison \
            libelf-dev libssl-dev libncurses-dev zlib1g-dev liblz4-tool \
            libxml2-utils rsync unzip dwarves file python3 ccache jq bc dos2unix kmod libdw-dev elfutils pigz aria2
          sudo apt-get clean
          echo "::endgroup::"

      - name: ⚡ 安装 ccache (ECS by cctv18)
        run: |
          set -euo pipefail
          echo "::group::Install ccache ECS"
          curl -LfsS --retry 5 --retry-delay 5 --retry-all-errors --connect-timeout 30 \
            -H "User-Agent: Mozilla/5.0" \
            "https://github.com/WildKernels/kernel_patches/raw/refs/heads/main/ccache/ccache-x86-64" -o ccache
          sudo cp -f ./ccache /usr/bin/ccache
          sudo chmod +x /usr/bin/ccache
          rm -f ./ccache
          echo "ccache version: $(ccache --version | head -1)"
          echo "::endgroup::"

      - name: ♻️ 配置 ccache 与 LTO 缓存路径
        run: |
          set -euo pipefail
          echo "::group::Configure ccache env"
          CACHE_DIR="${{ github.workspace }}/.ccache"
          LDCACHE_DIR="${{ github.workspace }}/.ld_cache"
          CCACHE_LOG="${{ github.workspace }}/ccache.log"
          mkdir -p "$CACHE_DIR" "$LDCACHE_DIR"

          SETTINGS=(
            "CCACHE_DIR=$CACHE_DIR"
            "LDCACHE_DIR=$LDCACHE_DIR"
            "CCACHE_MAXSIZE=12G"
            "CCACHE_COMPILERCHECK=content"
            "CCACHE_BASEDIR=${GITHUB_WORKSPACE}"
            "CCACHE_NOHASHDIR=true"
            "CCACHE_IGNOREOPTIONS=--sysroot*"
            "CCACHE_COMPRESSION=true"
            "CCACHE_COMPRESSION_LEVEL=3"
            "CCACHE_DIRECT=true"
            "CCACHE_FILE_CLONE=true"
            "CCACHE_INODE_CACHE=true"
            "CCACHE_IS_KERNEL_COMPILING=true"
            "CCACHE_UMASK=002"
            "CCACHE_SLOPPINESS=file_macro,time_macros,include_file_mtime,include_file_ctime,pch_defines,system_headers,locale"
          )
          for setting in "${SETTINGS[@]}"; do
            export "$setting"
            echo "$setting" >> "$GITHUB_ENV"
          done

          if [ "${{ inputs.debug }}" = "true" ]; then
            echo "CCACHE_LOGFILE=$CCACHE_LOG" >> "$GITHUB_ENV"
          fi
          if ccache --help 2>&1 | grep -q 'depend_mode'; then
            echo "CCACHE_DEPEND=true" >> "$GITHUB_ENV"
          fi

          echo "✅ ccache configured"
          ccache -p
          echo "::endgroup::"

      - name: 🧹 准备 op_config_json（去掉 KSU 字段，交给 action 传参处理）
        id: prepare_config
        shell: bash
        run: |
          echo "config_json=$(jq -nc --argjson m '${{ toJSON(matrix) }}' '$m | del(.ksu_type, .ksu_hash, .ksu_resolved_hash, .susfs_resolved_hash)')" >> "$GITHUB_OUTPUT"

      - name: 🔨 编译内核（启用 KPM=${{ inputs.enable_kpm }}）
        id: build
        uses: ./.github/actions/build-kernel
        with:
          op_config_json: ${{ steps.prepare_config.outputs.config_json }}
          ksu_type: ${{ matrix.ksu_type }}
          ksu_branch_or_hash: ${{ matrix.ksu_resolved_hash }}
          ksu_version_override: ${{ inputs.ksu_version_override }}
          susfs_commit_hash_or_branch: ${{ matrix.susfs_resolved_hash }}
          optimize_level: ${{ inputs.optimize_level }}
          enable_kpm: ${{ inputs.enable_kpm }}
          clean: ${{ inputs.clean_build }}
          debug: ${{ inputs.debug }}
          build_timestamp: ${{ inputs.build_timestamp }}
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: 📊 构建摘要
        if: always()
        run: |
          {
            echo ""
            echo "### ${{ matrix.model }} (${{ matrix.os_version }}) — ${{ job.status == 'success' && '✅ 成功' || '❌ 失败' }}"
            echo ""
            if [ "${{ steps.build.outcome }}" = "success" ]; then
              echo "| 指标 | 值 |"
              echo "|------|------|"
              echo "| **内核版本** | ${{ steps.build.outputs.kernel_version }} |"
              echo "| **KSU 版本** | v${{ steps.build.outputs.ksu_version }} |"
              echo "| **SUSFS 版本** | ${{ steps.build.outputs.susfs_version }} |"
              echo "| **KPM 启用** | ${{ inputs.enable_kpm && '✅ 是' || '❌ 否' }} |"
              echo "| **构建耗时** | ${{ steps.build.outputs.build_time }}s |"
              echo "| **Image SHA256** | \`${{ steps.build.outputs.image_sha256 }}\` |"
              echo "| **ZIP SHA256** | \`${{ steps.build.outputs.zip_sha256 }}\` |"
              echo "| **ccache 命中率** | ${{ steps.build.outputs.ccache_hit_rate }} |"
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  # ============================================================
  # 第 3 阶段：可选 Release（make_release=true 才执行）
  # ============================================================
  release:
    name: 🚀 创建 Release
    needs: [resolve-config, build]
    if: |
      !cancelled() &&
      inputs.make_release == true &&
      needs.build.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: 📥 下载构建产物
        uses: actions/download-artifact@v8
        with:
          path: ./artifacts
          merge-multiple: true
          skip-decompress: true

      - name: 🏷️ 生成 Release Tag
        id: tag
        run: |
          SUSFS_VER="${{ needs.resolve-config.outputs.susfs_version }}"
          BASE_TAG="${SUSFS_VER:-custom}-KPM"
          # 找已有同前缀 tag 决定 -rN 后缀
          LATEST=$(gh api repos/${{ github.repository }}/tags --jq "[.[] | select(.name | startswith(\"${BASE_TAG}-r\"))] | sort_by(.name | split(\"-r\")[-1] | tonumber) | last | .name // empty")
          if [ -z "$LATEST" ]; then
            NEW_TAG="${BASE_TAG}-r1"
          else
            NEW_TAG=$(echo "$LATEST" | awk -F'-r' '{printf "%s-r%d", $1, $NF+1}')
          fi
          echo "NEW_TAG=$NEW_TAG" >> "$GITHUB_ENV"
          echo "tag=$NEW_TAG" >> "$GITHUB_OUTPUT"

      - name: 📝 生成 Release 说明
        run: |
          cat > release_notes.md << EOF
          # 🎯 一加内核 — ${{ inputs.ksu_type }} + SUSFS ${{ needs.resolve-config.outputs.susfs_version }}${{ inputs.enable_kpm && ' + KPM' || '' }}

          > **构建时间**：$(date -u '+%Y-%m-%d %H:%M:%S UTC')
          > **构建 ID**：\`${{ github.run_id }}\`

          ## 📦 设备
          - **${{ inputs.device_config }}**

          ## 🔧 构建配置
          | 项目 | 值 |
          |------|------|
          | KSU 类型 | ${{ inputs.ksu_type }} |
          | KSU 引用 | \`${{ inputs.ksu_branch_or_hash }}\` |
          | KPM 启用 | ${{ inputs.enable_kpm && '✅ 是（已 patch_linux 注入 Image）' || '❌ 否' }} |
          | SUSFS 版本 | ${{ needs.resolve-config.outputs.susfs_version }} |
          | 优化等级 | ${{ inputs.optimize_level }} |

          ## ✨ 特性
          - **${{ inputs.ksu_type }}** — 内核级 Root 方案
          - **SUSFS** — 高级 Root 隐藏
          ${{ inputs.enable_kpm && '- **KPM** — 运行时加载 .kpm 内核补丁，可挡进程内存扫描类反 root 检测' || '' }}

          ## 📥 刷入说明
          1. 已 unlock bootloader
          2. 备份现有 boot.img
          3. 在已有 Root 环境下用 Kernel Flasher 刷入下方 AnyKernel3 ZIP

          ---
          ⚡ 由 GitHub Actions 自动构建
          EOF

      - name: 🚀 创建 GitHub Release
        run: |
          gh release create "${{ env.NEW_TAG }}" \
            --repo "${{ github.repository }}" \
            --title "一加内核 ${{ inputs.ksu_type }}${{ inputs.enable_kpm && ' + KPM' || '' }} (${{ env.NEW_TAG }})" \
            --notes-file release_notes.md

      - name: 📤 上传产物到 Release
        run: |
          for f in ./artifacts/*.zip; do
            [ -f "$f" ] || continue
            echo "Uploading $(basename "$f")..."
            gh release upload "${{ env.NEW_TAG }}" "$f" --clobber
          done
